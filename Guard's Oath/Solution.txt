1. Load `Oath.exe` in IDA/Ghidra/Cutter.
2. Find the validation function that:
   - transforms 9 input bytes in a loop
   - compares the result to a constant 9-byte table.
3. Recreate that transform in a script and invert it per byte:
   - inverse rotate right
   - subtract index-based value
   - xor index-based mask.
4. Recovered oath: `B0N3_WARD`
5. Run the binary and input `B0N3_WARD` to print the flag.
FLAG: {_0ATH_BR3KER_}